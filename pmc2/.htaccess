# Disable PHP output buffering so SSE chunks reach the browser in real-time.
# X-Accel-Buffering: no (set in PHP headers) is nginx-only; these cover LiteSpeed.
php_value output_buffering 0
php_flag  implicit_flush   on
php_flag  zlib.output_compression off
